apiVersion: v1
kind: Service
metadata:
  name: seat-service
  namespace: ticketing
spec:
  selector:
    app: seat-service
  ports:
    - protocol: TCP
      port: 3001
      targetPort: 3001
  type: ClusterIP
